import 'phaser';
import { assert } from 'chai';

describe('Background', () => {
    it('should scroll down as player moves up', () => {
        assert.fail('Not implemented');
    });
    it('should load a new tile if it scrolled down past threshold', () => {
        assert.fail('Not implemented');
    });
});
